apiVersion: v1
kind: Service
metadata:
  name: {{ template "chatroom.fullname" . }}
  labels:
    app: {{ template "chatroom.name" . }}
    chart: {{ template "chatroom.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "chatroom.name" . }}
    release: {{ .Release.Name }}
